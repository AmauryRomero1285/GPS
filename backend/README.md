# API to backend

this readme will declare the structure to create the backend in thsi project

## API docs

With the server running (`npm run dev` / `npm start`), the Swagger UI panel is available at `/api-docs` and the raw OpenAPI spec at `/api-docs.json`.
